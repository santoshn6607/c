#include <stdio.h>

int main() {
    char name[50];
    int u;
    float bill = 100;

    printf("Name: ");
    scanf("%s", name);
    printf("Units: ");
    scanf("%d", &u);

    bill += u <= 200 ? u * 0.8 : u <= 300 ? 200 * 0.8 + (u - 200) * 0.9 : 200 * 0.8 + 100 * 0.9 + (u - 300);

    if (bill > 400) bill *= 1.15;

    printf("\n-- Bill --\nName: %s\nUnits: %d\nTotal: ₹%.2f\n", name, u, bill);
    return 0;
}

***************************************************output*************************************************************************

Example Input:

Name: Ravi
Units: 350


Example Output:

-- Bill --
Name: Ravi
Units: 350
Total: ₹467.25
